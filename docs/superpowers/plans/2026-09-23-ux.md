# UX (sub-proyecto 4, v3.6.0) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Orden por columnas en plantilla, filtros min/max en overlay + solo libres, teclado en opciones/guardar, diálogo de salida en INICIO, barra de atajos global, nota en vivo en formación y los pendientes de prepartido/editor/foco.

**Architecture:** Cambios locales por pantalla + dos registros centrales (`ui/atajos.py` dibujado por `main.py`). Lógica testeable separada de la UI donde se pueda (`ordenar_plantilla`, `negociacion.filtrar`, funciones `_navegar` por pantalla).

**Tech Stack:** Python 3.10+, Pygame; tests con runner propio.

**Spec:** `docs/superpowers/specs/2026-09-23-ux-design.md`

## Global Constraints
- Comentarios `# v3.6.0: ...`; try/except + logger en hooks; sin commits.
- Suite: `for f in tests/test_*.py; do SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy PYTHONIOENCODING=utf-8 python "$f" > /dev/null 2>&1 || echo "FALLA $f"; done`.
- Nada se dibuja en y ≥ 698 salvo la barra de atajos (franja 698-720).
- Otros subagentes pueden estar trabajando en paralelo: el del sub-proyecto 3 (ofertas_screen, negociacion_screen, correo_screen, plantilla_screen:_dibujar_ficha, negociacion.py, market.py, contraofertas.py) y el del 5 (menu.py: loaders, PAISES/TIPOS_LIGA/CONFIGURACION_LIGAS; data/*; edit_screen pestañas de ligas). Tocar esos archivos solo en las zonas de este plan; si un Edit falla por cambios, releer y reintentar.

## Review Focus
- Escribir en el overlay de filtros con el buscador abierto → las teclas no disparan atajos del hub ni de volumen. Test en Task 2 (`test_filtros_teclas_no_escapan`).
- ESC en el diálogo de salida con la partida sin slot activo → GUARDAR Y SALIR abre la pantalla de guardado en vez de fallar. Test en Task 3 (`test_salir_sin_slot`).
- Pantalla nueva sin entrada en ATAJOS (p. ej. del sub-proyecto 6) → la barra muestra al menos "H Ayuda · ESC Volver", no se rompe. Test en Task 4 (`test_atajos_por_defecto`).
- Ordenar por columna con jugadores sin contrato/valor (0/None) → no lanza, quedan al final. Test en Task 1 (`test_orden_con_nulos`).
- Clic en un ítem del dropdown de rasgo justo sobre RESTAURAR BASE → cambia el rasgo, la base no se toca. Test en Task 5 (`test_dropdown_rasgo_no_restaura`).

## Reparto
Un subagente, Tasks 1-5 en orden.

---

### Task 1: Orden por columnas en PLANTILLA
**Files:** Modify `alpha_football/ui/plantilla_screen.py` (`ordenar_plantilla` ~69, encabezados, eventos). Test: `tests/test_ux_v360.py` (nuevo, cabecera como `tests/test_directiva_v280.py`).
**Produces:** `ordenar_plantilla(jugadores, orden: str, desc: bool = False) -> list` con claves `'pos','nombre','media','pot','edad','valor','contrato','moral','energia'`; `COLUMNAS: list[tuple[clave, titulo, x]]`; `rect_columna(clave) -> Rect`.

- [ ] Tests:
```python
from alpha_football.ui import plantilla_screen as PS

def test_orden_por_columna():
    e = estado_carrera(); js = e['mi_equipo'].jugadores
    for clave in ('pos', 'nombre', 'media', 'pot', 'edad', 'valor', 'contrato', 'moral', 'energia'):
        a = PS.ordenar_plantilla(js, clave, False); b = PS.ordenar_plantilla(js, clave, True)
        assert len(a) == len(js) and a != [] and (a == list(reversed(b)) or clave in ('pos',))

def test_click_encabezado_invierte():
    e = estado_carrera()
    click(PS.rect_columna('edad').center); PS.render(screen, e)
    assert e['plantilla_orden'] == ('edad', False)
    click(PS.rect_columna('edad').center); PS.render(screen, e)
    assert e['plantilla_orden'] == ('edad', True)

def test_orden_con_nulos():
    e = estado_carrera(); js = e['mi_equipo'].jugadores
    js[0].valor = None; js[1].contrato_anios = None
    PS.ordenar_plantilla(js, 'valor', True); PS.ordenar_plantilla(js, 'contrato', False)
```
- [ ] Implementar: claves con `key` tolerante (`int(x or 0)`), 'pos' ordena por `_ORDEN_POS` y luego media desc (por eso la reversa no es exacta). Encabezados clicables con ▲/▼ (usar "^"/"v" si la fuente no tiene el glifo — verificar con `get_font(...).size`/render). Mantener compatibilidad con las claves de orden viejas si otros módulos/tests las usan (`grep -rn "ordenar_plantilla" alpha_football tests`).
- [ ] Test verde + suite.

### Task 2: Filtros min/max en overlay + solo agentes libres
**Files:** Modify `alpha_football/negociacion.py:62-88` (`filtrar`), `alpha_football/ui/buscador_screen.py`. Test: `tests/test_ux_v360.py`.
**Produces:** `filtrar(pool, filtros)` con claves nuevas `edad_min, edad_max, media_min, media_max, pot_min, pot_max, precio_min, precio_max, solo_libres`; en buscador: `R_FILTROS`, `R_SOLO_LIBRES`, `estado['filtros_abierto']`, `rect_campo(clave) -> Rect`, `R_APLICAR`, `R_LIMPIAR`.
- [ ] Tests:
```python
from alpha_football import negociacion as N

def test_filtrar_min_max_y_libres():
    e = estado_carrera(); pool = N.pool_buscador(e)
    r = N.filtrar(pool, {'edad_min': 20, 'edad_max': 25, 'media_min': 70, 'media_max': 80})
    assert r and all(20 <= f['jugador'].edad <= 25 and 70 <= f['jugador'].overall <= 80 for f in r)
    libres = N.filtrar(pool, {'solo_libres': True})
    assert all(f['liga'] == N.LIBRES for f in libres)
    assert N.filtrar(pool, {'media_min': 60}) == N.filtrar(pool, {'media_min': 60, 'solo_libres': False})

def test_filtros_overlay_aplicar():
    from alpha_football.ui import buscador_screen as B
    e = estado_carrera(); pygame.event.clear(); B.render(screen, e)
    click(B.R_FILTROS.center); B.render(screen, e); assert e['filtros_abierto']
    click(B.rect_campo('edad_max').center); B.render(screen, e)
    for d in '23':
        pygame.event.clear(); pygame.event.post(pygame.event.Event(pygame.KEYDOWN, key=getattr(pygame, f'K_{d}'), mod=0, unicode=d)); B.render(screen, e)
    click(B.R_APLICAR.center); B.render(screen, e)
    assert not e['filtros_abierto'] and all(f['jugador'].edad <= 23 for f in e['busq_resultados'])

def test_filtros_teclas_no_escapan():
    from alpha_football.ui import buscador_screen as B
    e = estado_carrera(); e['filtros_abierto'] = True
    pygame.event.clear(); pygame.event.post(pygame.event.Event(pygame.KEYDOWN, key=pygame.K_ESCAPE, mod=0, unicode=''))
    assert B.render(screen, e) is None and not e['filtros_abierto']      # ESC cierra el overlay, no sale
```
(Ajustar los nombres de campos del resultado del pool — `f['jugador']`, `f['liga']` — a los que devuelve `pool_buscador` hoy; leerlo antes.)
- [ ] Implementar: overlay centrado 760×420 con filas min/max (edad, media, potencial, precio) + posición/liga (ciclan con clic), foco en un campo a la vez (clic), dígitos con `ui/entrada_monto.editar_valor` si ya existe (sub-proyecto 3), si no, crear ese módulo con esa función (idéntica a la del plan del 3). Mientras el overlay está abierto `estado['texto_activo'] = True`. `busq_resultados` se recalcula al aplicar. El toggle SOLO AGENTES LIBRES en la barra superior.
- [ ] Verde + suite.

### Task 3: Teclado en OPCIONES y GUARDAR + diálogo de salida en INICIO + foco tras copa
**Files:** Modify `alpha_football/ui/options_screen.py`, `alpha_football/ui/save_slots_screen.py`, `alpha_football/ui/league_screen.py` (manejo de ESC en INICIO; foco al volver). Test: `tests/test_ux_v360.py`.
**Produces:** en options: `estado['opt_foco']: int`, `ITEMS_OPCIONES: list[str]`; en save_slots: `estado['slot_foco']`; en league_screen: `estado['dialogo_salir']: bool`, `R_SALIR_GUARDAR`, `R_SALIR_SIN`, `R_SALIR_CANCELAR`.
- [ ] Tests:
```python
def key(k, uni=''):
    pygame.event.clear(); pygame.event.post(pygame.event.Event(pygame.KEYDOWN, key=k, mod=0, unicode=uni))

def test_opciones_teclado():
    from alpha_football.ui import options_screen as O
    e = estado_carrera(); e['opt_foco'] = 0
    key(pygame.K_DOWN); O.render(screen, e); assert e['opt_foco'] == 1
    key(pygame.K_UP); O.render(screen, e); assert e['opt_foco'] == 0
    key(pygame.K_ESCAPE); assert O.render(screen, e) is not None

def test_guardar_teclado():
    from alpha_football.ui import save_slots_screen as S
    e = estado_carrera(); e['slot_foco'] = 0
    key(pygame.K_DOWN); S.render(screen, e); assert e['slot_foco'] == 1

def test_dialogo_salir():
    from alpha_football.ui import league_screen as L
    e = estado_carrera(); e['hub_tab'] = 'inicio'
    key(pygame.K_ESCAPE); L.render(screen, e); assert e['dialogo_salir']
    key(pygame.K_ESCAPE); L.render(screen, e); assert not e['dialogo_salir']          # cancelar
    key(pygame.K_ESCAPE); L.render(screen, e)
    key(pygame.K_s, 's'); assert L.render(screen, e) == 'menu'                          # sin guardar

def test_salir_sin_slot():
    from alpha_football.ui import league_screen as L
    e = estado_carrera(); e['hub_tab'] = 'inicio'; e.pop('slot_activo', None); e['dialogo_salir'] = True
    key(pygame.K_RETURN); assert L.render(screen, e) == 'save_slots_screen'

def test_esc_en_otra_pestana_vuelve_a_inicio():
    from alpha_football.ui import league_screen as L
    e = estado_carrera(); e['hub_tab'] = 'oficina'
    key(pygame.K_ESCAPE); L.render(screen, e); assert e['hub_tab'] == 'inicio' and not e.get('dialogo_salir')
```
(Si `league_screen.render` necesita más estado para correr — copa, calendario — construirlo como hacen `tests/test_hub_v240.py`.) GUARDAR Y SALIR con `slot_activo`: `save.guardar_en_slot(...)` como hace el botón GUARDAR del hub y vuelve a `'menu'`; sin slot → `'save_slots_screen'` con `estado['salir_tras_guardar'] = True`.
- [ ] Foco tras copa: al entrar al hub desde `copa_screen`/`match_screen` de copa, `estado['hub_foco'] = 'jugar'` (leer cómo se maneja hoy el foco de teclado del hub y fijarlo ahí). Test: `test_foco_jugar_tras_copa` (poner `estado['pantalla_anterior']='copa_screen'`, render, `assert e['hub_foco'] == 'jugar'` — adaptar al nombre real de la variable de foco).
- [ ] Verde + suite.

### Task 4: Barra de atajos global + nota en vivo en formación
**Files:** Create `alpha_football/ui/atajos.py`; Modify `main.py` (dibujar la barra tras el render), `alpha_football/ui/team_screen.py` (nota en vivo). Test: `tests/test_ux_v360.py`.
**Produces:** `atajos.texto(nombre_pantalla, estado) -> str`, `atajos.dibujar(screen, nombre_pantalla, estado) -> None`, `ATAJOS: dict[str, str]`, `SIN_BARRA = {'menu', 'match_screen'}`.
- [ ] Tests:
```python
def test_atajos_todas_las_pantallas():
    import main
    from alpha_football.ui import atajos as A
    for nombre in main.nombres_pantallas():
        if nombre in A.SIN_BARRA: continue
        t = A.texto(nombre, {})
        assert "H Ayuda" in t and len(t) <= 150, nombre

def test_atajos_por_defecto():
    from alpha_football.ui import atajos as A
    assert A.texto('pantalla_que_no_existe', {}) == "H Ayuda · ESC Volver"
    A.dibujar(screen, 'league_screen', {})

def test_nota_en_vivo():
    from alpha_football.ui import team_screen as T
    e = estado_carrera(); j = e['mi_equipo'].alineacion_activa.titulares[0]
    assert T.texto_nota_vivo(j, {'notas_vivo': {j.id: 7.8}}) == ("7.8", 'verde')
    assert T.texto_nota_vivo(j, {}) is None
```
`main.nombres_pantallas()` = función nueva que devuelve la lista de claves de `PANTALLAS` sin inicializar la ventana (mover la tabla nombre→módulo a una constante `MODULOS_PANTALLA = {'menu': 'alpha_football.ui.menu', ...}` a nivel de módulo y construir `PANTALLAS` desde ella). Leer primero cómo `match_screen` guarda las notas en vivo (buscar `nota` en `match_screen.py`/`engine.py`) y usar esa fuente en `texto_nota_vivo`; el test usa `estado['notas_vivo']` como ejemplo — ajustar a la clave real.
- [ ] Textos de ATAJOS: uno por pantalla, con los atajos reales de cada una (leer los KEYDOWN de cada pantalla). Formato "Tecla Acción · Tecla Acción · H Ayuda".
- [ ] Barra: franja `Rect(0, 698, 1280, 22)` semitransparente, texto `size='sm'` centrado. Revisar con capturas headless de hub, plantilla, buscador, team_screen y finanzas que nada importante quede debajo; mover lo que pise la franja.
- [ ] Verde + suite.

### Task 5: Pendientes de prepartido y editor
**Files:** Modify `alpha_football/ui/prepartido_screen.py`, `alpha_football/ui/edit_screen.py`. Test: `tests/test_ux_v360.py`.
- [ ] Tests:
```python
def test_prepartido_botones_caben():
    from alpha_football.ui import prepartido_screen as P
    from alpha_football.ui.theme import get_font
    for texto, rect in P.botones_menu():
        assert get_font('md').size(texto)[0] <= rect.width - 16, texto

def test_dropdown_rasgo_no_restaura():
    from alpha_football.ui import edit_screen as ED
    e = {}; ED.render(screen, e)                                   # inicializa el editor
    e['edit_dropdown_activo'] = 'rasgo'
    llamado = []
    orig = ED.restaurar_base; ED.restaurar_base = lambda *a, **k: llamado.append(1)
    try:
        click(ED.R_RESTAURAR.center); ED.render(screen, e)
    finally:
        ED.restaurar_base = orig
    assert not llamado
```
`P.botones_menu()` = lista `(texto, rect)` de los botones numerados del prepartido (extraer de su render). En el editor, extraer la acción de RESTAURAR BASE a `restaurar_base(estado)` y su rect a `R_RESTAURAR` si hoy están inline; con cualquier dropdown abierto, el clic se consume en el dropdown (o lo cierra si cae fuera) y no llega a botones de abajo; dibujar los paneles inferiores ANTES del dropdown.
- [ ] Glifo en cuadro: buscar en prepartido caracteres fuera de la fuente (emojis, ⚽, ▶…) y reemplazarlos por texto/formas dibujadas.
- [ ] Verde + suite + capturas headless de prepartido y editor (dropdown abierto).
